/** When `VITE_SHOW_TRIPS` is the string `"false"`, trip listings and nav are hidden. Unset or any other value keeps trips visible. */
export const showTrips = import.meta.env.VITE_SHOW_TRIPS !== "false";
