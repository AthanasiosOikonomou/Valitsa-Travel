/** Must match `storageKey` on next-themes `ThemeProvider` and the inline script in `index.html`. */
export const THEME_STORAGE_KEY = "valitsa-theme";

/** Legacy key from the pre–next-themes `ThemeContext`. */
export const LEGACY_DARK_MODE_KEY = "darkMode";
